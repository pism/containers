This repository contains definition files and scripts needed to build
PISM containers that can be used on some HPC systems.

So far only Apptainer (Singularity) containers are supported.

See the `singularity` directory for more.
